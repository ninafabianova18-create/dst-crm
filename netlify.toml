[build]
  command = "if [ -d dst-crm ]; then npm --prefix dst-crm run build && rm -rf dist && cp -r dst-crm/dist dist; else npm run build; fi"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200